module.exports = {
  // Punto y coma al final de las declaraciones
  semi: true,

  // Comillas simples en lugar de dobles
  singleQuote: true,

  // Ancho de tabulación (2 espacios)
  tabWidth: 2,

  // Usar espacios en lugar de tabs
  useTabs: false,

  // Ancho máximo de línea antes de quebrar
  printWidth: 100,

  // Comas finales en objetos y arrays (compatibilidad ES5)
  trailingComma: 'es5',

  // Espacios en llaves de objetos: { foo: bar }
  bracketSpacing: true,

  // Flecha de funciones: siempre paréntesis (x) => x
  arrowParens: 'always',

  // Fin de línea: auto (según el sistema)
  endOfLine: 'auto',
};
